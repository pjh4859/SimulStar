function [results] = ShowLog(SvectorFlag)
%SHOWLOG 이 함수의 요약 설명 위치
%   자세한 설명 위치
% temp = [strcat('RA:',string(Attitude.RA),'  DEC:',string(Attitude.DEC), '  ROT:',string(Attitude.ROT), '(deg)')];

results = string(SvectorFlag);
end

